import React from "react";

const WorkshopThreadManager = () => {
    return (
        <React.Fragment>
            <div className="workshop-page__title">Threads</div>
        </React.Fragment>
    );
}

export default WorkshopThreadManager;